import React from 'react'
import Samplepage from '../components/Samplepage'

function SamplePage({theme, show}) {
  return (
    <Samplepage theme={theme} show={show} />
  )
}

export default SamplePage